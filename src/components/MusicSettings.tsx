import { Box, Typography } from '@mui/material';

export default function MusicSettings() {
  return (
    <Box>
      <Typography variant="h6" sx={{ mb: 2 }}>Settings</Typography>
      <Typography>Music settings unavailable.</Typography>
    </Box>
  );
}
